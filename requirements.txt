# Loovi 런타임 의존성 (GPU/CUDA 환경 기준, Python 3.11)
# 현재 환경에 검증된 버전으로 핀. CPU 전용 환경이면 아래 onnxruntime 주석 참고.

# --- 영상 / 수치 ---
numpy==2.1.0
opencv-python==4.13.0.92
PyYAML==6.0.3

# --- person detection / tracking ---
# ultralytics 가 BoT-SORT / ByteTrack tracker 와 torch 를 함께 끌어온다.
ultralytics==8.4.82
# BoT-SORT의 선형 할당기. 없으면 tracker 초기화가 실패해 custom tracker로 폴백한다.
lap==0.5.13

# --- ONNX 추론 (person YOLO + face) ---
onnx==1.22.0
# GPU 추론용. CPU 전용이면 이 줄을 지우고 onnxruntime 로 교체할 것.
# 주의: onnxruntime(CPU) 과 onnxruntime-gpu 를 동시에 설치하면 충돌한다.
onnxruntime-gpu==1.26.0

# --- face enrichment (검출 + 성별/연령) ---
# 최초 실행 시 buffalo_l 모델 팩을 ~/.insightface/models/ 로 자동 다운로드.
# 주의: insightface 는 의존성으로 CPU onnxruntime 을 끌어온다.
#       설치 후 반드시 'pip uninstall -y onnxruntime' 로 제거해 GPU 와의 충돌을 막을 것.
insightface>=1.0.1

# --- gaze 2차: head pose 모델은 PIP 비제공 ---
# DMHead ONNX 를 PINTO0309/DMHead 릴리스에서 받아 models/dmhead.onnx 로 둘 것.

# --- realtime 2차: SQS 전송 ---
# realtime.enable=false 면 불필요. AWS 자격증명은 표준 방식(환경변수/IAM)으로 주입.
boto3>=1.34
jsonschema>=4.0
